datamodule:
  dataset_path: "${oc.env:HOME}/bigearthnet/bigearthnet/data/bigearthnet-mini/"
  batch_size: 16
  num_workers: 0

logger:
  save_dir: "../../tb_logs/"
  name: "bigearth"

trainer:
  max_epochs: 5

defaults:
  - model: baseline.yaml
